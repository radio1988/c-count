#BSUB -J "ccount"  # n jobs in array, batch size
#BSUB -R rusage[mem=8000]  # for each cpu
#BSUB -n 1  # n cpu
#BSUB -R "span[hosts=1]" # All hosts on the same chassis"
#BSUB -o submit.%J.%I.log
#BSUB -W 144:00
#BSUB -q long
#BSUB -N

source activate ccount
snakemake -k -p --jobs 999 --latency-wait 60 \
--cluster 'bsub -q short -o lsf.log -R "rusage[mem={params.mem}]" -n {threads} -R span[hosts=1] -W 4:00'

snakemake -j 1 --report report.html
